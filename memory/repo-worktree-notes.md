# Repo Worktree Notes

## Current Observations

- The repo contains active production code and many in-flight changes
- `OpenMAIC/` is cloned locally for analysis
- `AI-Tutor-Backend/` and `AI-Tutor-Frontend/` were added as new workspaces
- root-level cleanup removed report/PDF/APK/PowerShell files earlier in this session

## Working Practice

- assume the worktree can be dirty
- avoid reverting unrelated changes
- verify the exact file ownership before editing
- prefer source-backed updates over inferred summaries
